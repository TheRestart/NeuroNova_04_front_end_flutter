# Mesothelioma 앱 설정 변경 가이드

## 📱 앱 이름 및 아이콘 변경 내역

이 문서는 2025년 11월 13일에 수행된 앱 설정 변경 작업을 기록합니다.

---

## 1. 앱 이름 변경

### ✅ 변경 사항

**이전 이름**: `flutter_diease_programming`
**새 이름**: `Mesothelioma`

### 📝 수정된 파일

#### Android 앱 표시 이름
**파일**: `android/app/src/main/AndroidManifest.xml`
**라인**: 3

```xml
<application
    android:label="Mesothelioma"
    android:name="${applicationName}"
    android:icon="@mipmap/ic_launcher">
```

**변경 내용**:
- `android:label` 속성을 `"flutter_diease_programming"`에서 `"Mesothelioma"`로 변경
- 이 설정은 Android 기기의 앱 런처에 표시되는 이름입니다

### 📌 참고 사항

다음 파일들은 **변경하지 않았습니다** (Android 전용 설정이므로 불필요):

1. **pubspec.yaml** (line 1)
   ```yaml
   name: flutter_diease_app  # 변경 안 함
   ```
   - 이유: Flutter 프로젝트 패키지 이름으로 내부적으로만 사용됨
   - 사용자에게 표시되지 않음

2. **lib/main.dart** (line 28)
   ```dart
   title: 'mesothelium_flutter_app',  # 변경 안 함
   ```
   - 이유: 웹 브라우저 탭 제목 등에만 사용됨
   - Android 네이티브 앱에는 영향 없음

3. **ios/Runner/Info.plist**
   - 이유: iOS 플랫폼은 사용하지 않음

---

## 2. 앱 아이콘 변경

### 🎨 원본 이미지

**소스 파일**: `android/app/src/main/res/Mesothelioma.png`
**형식**: PNG
**용도**: 모든 해상도의 앱 아이콘 생성을 위한 원본 이미지

### 🛠️ 자동 생성 도구 사용

**패키지**: `flutter_launcher_icons` (v0.13.1)
**설치 날짜**: 2025년 11월 13일

#### pubspec.yaml 설정 추가

**파일**: `pubspec.yaml`
**라인**: 28, 63-68

```yaml
dev_dependencies:
  flutter_launcher_icons: ^0.13.1

flutter_launcher_icons:
  android: true
  ios: false
  image_path: "android/app/src/main/res/Mesothelioma.png"
  adaptive_icon_background: "#FFFFFF"
  adaptive_icon_foreground: "android/app/src/main/res/Mesothelioma.png"
```

**설정 설명**:
- `android: true` - Android 아이콘만 생성
- `ios: false` - iOS 아이콘 생성 안 함
- `image_path` - 원본 이미지 경로
- `adaptive_icon_background` - 흰색 배경 (#FFFFFF)
- `adaptive_icon_foreground` - 원본 이미지를 전경으로 사용

### 📦 생성된 아이콘 파일들

자동 생성 명령어:
```bash
flutter pub get
flutter pub run flutter_launcher_icons
```

#### 생성된 파일 목록

| 해상도 | 파일 경로 | 크기 (픽셀) | 파일 크기 | 용도 |
|--------|----------|------------|----------|------|
| mdpi | `android/app/src/main/res/mipmap-mdpi/ic_launcher.png` | 48 x 48 | 905 bytes | 저해상도 화면 |
| hdpi | `android/app/src/main/res/mipmap-hdpi/ic_launcher.png` | 72 x 72 | 1.4 KB | 중간 해상도 화면 |
| xhdpi | `android/app/src/main/res/mipmap-xhdpi/ic_launcher.png` | 96 x 96 | 1.8 KB | 고해상도 화면 |
| xxhdpi | `android/app/src/main/res/mipmap-xxhdpi/ic_launcher.png` | 144 x 144 | 3.0 KB | 매우 고해상도 화면 |
| xxxhdpi | `android/app/src/main/res/mipmap-xxxhdpi/ic_launcher.png` | 192 x 192 | 4.4 KB | 초고해상도 화면 |

#### Adaptive Icon 지원

**Android 8.0 (API 26) 이상**에서 지원되는 Adaptive Icon이 자동으로 생성되었습니다.

생성된 추가 파일:
- `android/app/src/main/res/values/colors.xml` (배경색 정의)
- Adaptive Icon 리소스 파일들

**Adaptive Icon 특징**:
- 런처의 형태(원형, 사각형, 둥근 사각형 등)에 맞춰 자동 조정
- 배경과 전경이 분리되어 애니메이션 효과 가능
- 다양한 Android 런처에서 일관된 모양 유지

---

## 3. 변경 사항 적용 방법

### 🔄 빌드 및 실행

앱을 다시 빌드하여 변경사항을 적용합니다:

```bash
# 이전 빌드 캐시 삭제
flutter clean

# APK 빌드
flutter build apk

# 또는 직접 실행
flutter run
```

### ✅ 확인 방법

1. **앱 이름 확인**:
   - Android 기기의 앱 서랍에서 "Mesothelioma" 이름 확인

2. **앱 아이콘 확인**:
   - 홈 화면 또는 앱 서랍에서 새로운 아이콘 확인
   - 다양한 런처에서 Adaptive Icon 동작 확인

---

## 4. 향후 변경 시 참고사항

### 앱 이름만 변경하는 경우

**Android**:
```xml
<!-- android/app/src/main/AndroidManifest.xml -->
<application android:label="새로운_앱_이름">
```

**iOS** (필요시):
```xml
<!-- ios/Runner/Info.plist -->
<key>CFBundleDisplayName</key>
<string>새로운_앱_이름</string>
```

### 앱 아이콘만 변경하는 경우

1. 새로운 원본 이미지를 준비 (권장: 512x512 이상의 PNG)
2. `android/app/src/main/res/` 폴더에 저장
3. `pubspec.yaml`의 `image_path` 수정:
   ```yaml
   flutter_launcher_icons:
     image_path: "android/app/src/main/res/새로운_이미지.png"
   ```
4. 아이콘 재생성:
   ```bash
   flutter pub run flutter_launcher_icons
   ```

### 배경색 변경

Adaptive Icon의 배경색을 변경하려면:

```yaml
flutter_launcher_icons:
  adaptive_icon_background: "#원하는_색상코드"
```

예시:
- 흰색: `"#FFFFFF"`
- 파란색: `"#255ED6"` (앱의 Primary Color)
- 투명: `"#00FFFFFF"`

---

## 5. 트러블슈팅

### 문제: 아이콘이 변경되지 않음

**해결 방법**:
```bash
flutter clean
flutter pub get
flutter pub run flutter_launcher_icons
flutter build apk
```

### 문제: 앱 이름이 변경되지 않음

**확인 사항**:
1. AndroidManifest.xml 파일이 올바르게 저장되었는지 확인
2. 앱을 완전히 삭제 후 재설치
3. 기기를 재부팅

### 문제: 아이콘 품질이 낮음

**해결 방법**:
- 원본 이미지를 더 고해상도로 준비 (최소 512x512, 권장 1024x1024)
- PNG 형식 사용 (투명 배경 지원)
- 이미지 압축을 최소화

---

## 📊 변경 전후 비교

| 항목 | 변경 전 | 변경 후 |
|------|---------|---------|
| 앱 표시 이름 (Android) | flutter_diease_programming | Mesothelioma |
| 앱 아이콘 | 기본 Flutter 아이콘 | 커스텀 Mesothelioma 아이콘 |
| Adaptive Icon 지원 | 미지원 | 지원 (Android 8.0+) |
| 아이콘 해상도 | 5개 | 5개 (자동 최적화됨) |

---

## ✅ 체크리스트

- [x] AndroidManifest.xml 앱 이름 변경
- [x] flutter_launcher_icons 패키지 설치
- [x] pubspec.yaml 아이콘 설정 추가
- [x] 원본 이미지 준비 (Mesothelioma.png)
- [x] 5개 해상도 아이콘 자동 생성
- [x] Adaptive Icon 생성
- [x] colors.xml 생성
- [ ] 실제 기기에서 테스트 (다음 단계)
- [ ] 다양한 Android 버전에서 확인 (다음 단계)

---

## 📝 작업 이력

| 날짜 | 작업 내용 | 담당자 |
|------|----------|--------|
| 2025-11-13 | 앱 이름 변경 (Mesothelioma) | Claude |
| 2025-11-13 | 앱 아이콘 자동 생성 및 적용 | Claude |
| 2025-11-13 | Adaptive Icon 설정 | Claude |

---

**참고 문서**:
- Flutter 공식 문서: https://docs.flutter.dev/deployment/android
- flutter_launcher_icons 패키지: https://pub.dev/packages/flutter_launcher_icons
- Android Adaptive Icons: https://developer.android.com/guide/practices/ui_guidelines/icon_design_adaptive
